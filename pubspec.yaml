name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^0.8.6
  audioplayers: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # เพิ่ม assets สำหรับเสียงที่นี่
  assets:
    - assets/click_sound.mp3 
    - assets/img1.png # เสียงคลิก
    # คุณสามารถเพิ่ม assets อื่นๆ ที่ต้องการได้ที่นี่
